{% comment %}
Renders an image background which supports high pixel density screens by default.

Accepts:

- desktop_image: {image} Image to display at desktop resolutions.
- (Optional) desktop_image_alt: {text} Custom alt text for the image displayed at desktop resolutions.
- (Optional) mobile_image: {image} Image to display at mobile resolutions.
- (Optional) mobile_image_alt: {text} Custom alt text for the image displayed at mobile resolutions.
- (Optional) desktop_large_image: {image} Image to display at large desktop resolutions.
- (Optional) desktop_large_image_alt: {text} Custom alt text for the image displayed at large desktop resolutions. 
- (Optional) no_lazy_load: {boolean} Set the lazy loading of this image banner to false


Usage:
{% render 'image-banner-media',
desktop_image: section.settings.desktop_image,
mobile_image: section.settings.mobile_image
%}

{% endcomment %}

<div class="banner__media media">
  {%- if desktop_large_image != blank -%}
  <div class="banner__media banner__media--large-screens medium-hide small-hide media">
    {% render 'image',
      image: desktop_large_image,
      image_max_width: 2800,
      image_desired_width: desktop_large_image.width,
      alt: desktop_large_image_alt,
      style: "max-width: 100%; height: 100%;",
      do_not_lazy_load: no_lazy_load,
      high_fetch_priority: high_fetch_priority,
    %}
  </div>
  {%- endif -%}

  {%- if desktop_image != blank -%}
    <div
      class="banner__media media {% if desktop_large_image != blank %}banner__media--large-screens--hide{% endif %} {% if mobile_image != blank %} small-hide{% endif %}">
      {% render 'image',
        image: desktop_image,
        image_max_width: 1400,
        image_desired_width: desktop_image.width,
        alt: desktop_image_alt,
        style: "max-width: 100%; height: 100%;",
        do_not_lazy_load: no_lazy_load,
        high_fetch_priority: high_fetch_priority,
      %}
    </div>
  {%- endif -%}

  {%- if mobile_image != blank -%}
    <div class="banner__media media large-up-hide medium-hide">
      {% render 'image',
        image: mobile_image,
        image_max_width: 400,
        image_desired_width: mobile_image.width,
        alt: mobile_image_alt,
        style: "max-width: 100%; height: 100%;",
        do_not_lazy_load: no_lazy_load,
        high_fetch_priority: high_fetch_priority,
      %}
    </div>
  {%- endif -%}
</div>
